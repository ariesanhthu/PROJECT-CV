\begin{tikzpicture}
    \begin{axis}[
        ybar,
        % --- CẤU HÌNH KÍCH THƯỚC CHO SLIDE ---
        width=14cm, height=5cm,       % Phình ngang để phù hợp slide
        bar width=18pt,               % Tăng độ rộng cột vì chỉ có 3 nhóm
        enlarge x limits=0.25,        % Căn lề thoáng hơn
        % -------------------------------------
        ymin=0, ymax=115,             % Đặt trần cao để chứa giá trị 99.6
        ylabel={Độ chính xác (\%)},
        % Định nghĩa trục X
        symbolic x coords={GSM8K, Sports, Coin Flip},
        xtick=data,
        % Nhãn trục X xuống dòng để rõ loại bài toán
        xticklabels={GSM8K\\(Số học), Sports\\(Thường thức), Coin Flip\\(Ký hiệu)},
        x tick label style={align=center, font=\small},
        % Hiển thị số liệu trên cột
        nodes near coords,
        every node near coord/.append style={
            font=\scriptsize, 
            /pgf/number format/fixed, 
            /pgf/number format/precision=1
        },
        % Cấu hình hiển thị trục
        axis x line*=bottom,
        axis y line*=left,
        ymajorgrids=true,
        grid style=dashed,
        tick label style={font=\footnotesize},
        % --- LEGEND NẰM DƯỚI & NGANG ---
        legend style={
            at={(0.5,-0.3)},          % Đưa xuống dưới trục X (thấp hơn xíu do nhãn 2 dòng)
            anchor=north,
            legend columns=-1,        % Xếp thành 1 hàng ngang
            font=\footnotesize,
            draw=none,
            cells={anchor=west},
            column sep=0.5cm          % Khoảng cách giữa các chú thích
        }
    ]
    % 1. Standard Prompting (Vàng nhạt)
    \addplot[fill=yellow!50, draw=black] coordinates {
        (GSM8K, 6.5) 
        (Sports, 59.5) 
        (Coin Flip, 49.0)
    };
    % 2. Variable Compute Only (Dùng pattern lưới chéo để khác biệt)
    \addplot[pattern=crosshatch, pattern color=black!70, draw=black] coordinates {
        (GSM8K, 6.4) 
        (Sports, 61.6) 
        (Coin Flip, 50.7)
    };
    % 3. Chain-of-Thought (Cam đậm)
    \addplot[fill=orange!80, draw=black] coordinates {
        (GSM8K, 14.3) 
        (Sports, 85.8) 
        (Coin Flip, 99.6)
    };
    \legend{Standard Prompting, Variable Compute, Chain-of-Thought}
    \end{axis}
\end{tikzpicture}