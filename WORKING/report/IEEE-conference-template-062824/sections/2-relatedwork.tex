% Nội dung Related Work đã được gom và viết lại hoàn chỉnh trong `sections/khaosat.tex`.
% File này được giữ lại để tránh đứt tham chiếu trong lịch sử chỉnh sửa của dự án,
% nhưng không còn được include trong `main.tex`.